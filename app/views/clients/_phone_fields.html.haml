.nested-fields
  = f.input :number, wrapper: :material_input
  = f.input :kind, label: false, collection: Phone::KINDS
  = f.input :available_from
  = f.input :available_to
  = link_to_remove_association "Delete phone", f, {class: 'btn red darken-4'}
