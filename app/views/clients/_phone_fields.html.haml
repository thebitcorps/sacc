.nested-fields
  = f.input :number
  = f.input :phone_type, collection: Phone::KINDS
  = f.input :available_from
  = f.input :available_to
  = link_to_remove_association "Remover numero  ", f, {class: 'btn btn-danger'}