= simple_form_for(@appointment) do |f|
  = f.error_notification

  .form-inputs
    = f.input :date, as: :string, input_html: { class: :datepicker}
    = f.input :time
    = f.input :place
    = f.input :status, collection: Appointment::STATUS

  .form-actions
    = f.button :submit
